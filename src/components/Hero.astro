<section class="relative min-h-screen flex items-center justify-center overflow-hidden bg-gradient-to-br from-blue-900 via-purple-900 to-black">
  <div id="particles" class="absolute inset-0"></div>
  <div class="relative z-10 text-center text-white px-6">
    <h1 class="text-5xl md:text-7xl font-bold mb-6 animate-fade-in-up">
      Envases que <span class="text-yellow-400">Venden</span>
    </h1>
    <p class="text-xl md:text-2xl mb-8 animate-fade-in-up animation-delay-200">
      Flexibles, sostenibles y 100% personalizados
    </p>
    <a href="#cotizar" class="bg-gradient-to-r from-yellow-400 to-orange-500 text-black font-bold py-4 px-8 rounded-full text-lg hover:scale-110 transform transition duration-300 animate-fade-in-up animation-delay-400">
      Cotizar Ahora →
    </a>
  </div>
</section>

<style>
  @keyframes fade-in-up {
    from { opacity: 0; transform: translateY(30px); }
    to { opacity: 1; transform: translateY(0); }
  }
  .animate-fade-in-up { animation: fade-in-up 1s ease-out forwards; }
  .animation-delay-200 { animation-delay: 0.2s; }
  .animation-delay-400 { animation-delay: 0.4s; }
</style>